package com.mom.privatedrawing

enum class Tool {
    PEN, PENCIL, MARKER, HIGHLIGHTER, ERASER,
    FILL, LINE, RECTANGLE, CIRCLE, TRIANGLE, STAR,
    TEXT, IMAGE
}

enum class ShapeType { LINE, RECTANGLE, CIRCLE, TRIANGLE, STAR }
